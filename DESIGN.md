---
name: Meeting Progress Tracker
version: 1
---

## Overview

面向单人项目负责人的高频行动台。以浅色背景、紧凑数据行、明确下一步为中心，继续使用 Django 模板和原生 JavaScript。首页先展示行动摘要；草稿默认展开异常、折叠可导入项；任务详情以记录进展为主要操作。

## Colors

CSS 变量定义于 `static/css/app.css`。正文 `--ink #17231d`，次级文字 `--muted #58665e`，页面 `--paper #f4f5f1`，辅助表面 `--surface #fbfcf9`，内容表面 `--white #ffffff`。绿色主操作 `--accent #28634c` 与 `--accent-dark #1e4d3b`，浅绿状态底色 `--accent-soft #e3eee8`。

错误、阻塞、延期采用 `--danger #a43d36` / `--danger-soft #f8e9e6`；需确认文本采用 `--warning #795510`。状态始终附中文标签，不仅依赖颜色。输入边界 `--line-strong #a5b2a8`，键盘焦点使用 3px 实色绿色轮廓。

## Typography

系统无衬线字体。固定层级：页面标题 26px、章节标题 18px、三级标题 15px、正文 15px、标签 13px、辅助信息 12px 左右。表格和指标使用等宽数字。原文、跟进文案及报告预览自动换行，长词允许断行；Markdown 编辑区使用等宽字体。

## Elevation

内容区以边框和留白分组，普通 surface 不使用阴影。导航使用实色背景。仅固定提交条保留轻量阴影帮助识别位置；移动端提交条回到文档流。没有渐变、玻璃模糊、装饰性入场或悬浮位移动效；保留 reduced-motion 保护。

## Components

- `surface`：表单、任务摘要、负责人跟进和周报内容的统一边界容器；`card` 是兼容旧模板的同类样式。
- `data-list` / `data-row`：项目、人员、任务时间线和首页队列，适合顺序扫描；相邻条目用分隔线分组。
- `summary-strip`：首页五项行动入口及草稿实时新增、更新、忽略、需确认汇总。
- `filter-chip`：草稿筛选和报告标签页；使用 `aria-pressed` 或 `aria-selected` 暴露选中状态。
- `empty-state`：解释当前空状态，并给出录入、清除筛选或确认等下一步。
- `sticky-action-bar`：草稿导入按钮同步实时计数，桌面贴近视窗底部，620px 以下回归正常流。
- 原生 `details`：紧凑审阅摘要；展开后才展示字段和差异。更改/批量接受处理方式后更新中文动作与计数；需确认基于实际字段有效性判断：新建/更新必须有项目、更新必须有目标、异常日期必须修正为合法 ISO 日期。未补齐的行保留在当前筛选中；明确忽略可解除。筛选不删除或禁用表单字段。
- 复制按钮统一使用 `data-copy-target`，成功或失败写入全站 `aria-live` 区域。
- 周报使用安全转义的预换行文本预览；键盘左右键、Home、End 和点击均可切换标签；无 JavaScript 时两份内容均可阅读。
- 任务表格具有标题、列头 scope 和可聚焦局部滚动区；进度条提供完整 ARIA 数值。主要目标至少 44px，900px 以下主要布局单栏，620px 以下表单单列。
- 快捷键在页脚可发现：`/` 聚焦任务搜索，`n` 新建会议；输入控件、编辑区域、组合输入和修饰键不触发快捷键。

## Do's and Don'ts

- 优先复用列表、摘要条和表面组件；让标题、状态、日期和下一步易于扫描。
- 保持原文可读、正式入库可控；批量操作保留服务器字段协议，无 JavaScript 时表单仍可提交。
- 所有状态变化提供文字反馈；日期使用语义化 time，进度条提供名称和数值。
- 不使用大标题、深色指标墙、重复悬浮卡片、装饰性动效或页面级横向滚动。
- 不通过 `innerHTML` 渲染会议原文或报告；不让单键快捷键抢占输入。
